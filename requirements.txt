# Core runtime dependencies — pinned to the versions this project is
# built and tested against. Mirrors the [project.dependencies] table in
# pyproject.toml, which is the source of truth; regenerate this file with
# `pip freeze` (or update the pins by hand) if you bump a version there.
#
# Install: pip install -r requirements.txt
# Equivalent to: pip install -e .
pandas==3.0.5
numpy==2.5.2
scipy==1.18.1
lightgbm==4.7.0
statsmodels==0.14.6
scikit-learn==1.9.0
pydantic==2.13.4
pydantic-settings==2.15.0
